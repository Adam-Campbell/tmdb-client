import React from 'react';
import PaginationControls from '../components/PaginationControls';

export default function Pagination(props) {
    return (
        <PaginationControls currentPage={80} />
    );
}